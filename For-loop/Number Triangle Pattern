#include <stdio.h>

int main() {
    int i, j;

    for(i = 1; i <= 5; i++) {      // controls rows
        for(j = 1; j <= i; j++) {  // prints numbers in each row
            printf("%d", j);
        }
        printf("\n");              // move to next line
    }

    return 0;
}
